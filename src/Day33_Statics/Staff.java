package Day33_Statics;

public class Staff {

    public String name;
    public double salary;
    public static boolean isHuman = true;
    /* is one copy of the variable enough/valid for every object?
        If so, we may use ----- static
     */

  //instance -- means it'll have more than one copy(without static) and each object is unique
  // static -- means it'll have one copy with the same value for every object
}
